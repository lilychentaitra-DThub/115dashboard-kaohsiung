# 企業進出口數據看板｜高雄場（5 分鐘版）

外貿協會高雄辦事處介紹用簡報，共 9 頁，預計 5 分鐘講完。

從《20260805_AiEZ TRADE論壇簡報》的 16 頁版本擷取後半段「企業進出口數據看板」（原第 08–16 頁）重新編排，
沿用同一套「貿易報單風・海關藍」設計系統。

**主辦**：經濟部國際貿易署　**執行**：中華民國對外貿易發展協會　數位科技中心

## 觀看

- 線上版：<https://lilychentaitra-dthub.github.io/115dashboard-kaohsiung/>
- 本機：直接用瀏覽器開啟 `index.html`（單檔、無外部相依、離線可用）
- PDF：`115dashboard-kaohsiung-slides.pdf`（9 頁，960×540pt＝PowerPoint 16:9）

現場建議用本機的 `index.html`，不要依賴會場網路。PDF 是給會後分送、或現場只能用他人電腦時的備案。

### PDF 怎麼重新產生

改完 `index.html` 之後，用無頭 Chrome 重印一次即可（頁面尺寸由 CSS 的 `@page` 決定）：

```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" `
  --headless=new --disable-gpu --no-pdf-header-footer `
  --run-all-compositor-stages-before-draw --virtual-time-budget=10000 `
  --print-to-pdf="115dashboard-kaohsiung-slides.pdf" "file:///<本資料夾絕對路徑>/index.html"
```

輸出是**可選取的真文字**（非圖片），字型已內嵌，換電腦開不會變形。

## 操作

| 動作 | 按鍵 |
|---|---|
| 下一頁 | `→` / `↓` / `Space` / `PageDown` |
| 上一頁 | `←` / `↑` / `PageUp` |
| 首頁 / 末頁 | `Home` / `End` |
| 講者備註開關 | `N` |
| 全螢幕 | `F` |

第 04、09 頁的印章是**獨立的一拍**：翻到該頁不會自動出現，講完內容再按一次方向鍵才蓋下來，
再按一次才翻頁；按上一頁會先收回印章。

## 頁次與配速（合計約 5 分）

| 頁 | 內容 | 秒數 | 說明 |
|---|---|---|---|
| 01 | 封面 | 25 | 深藍反白；四格條件（999／無限次／每月更新／限量 300 家）＋文字署名。不放 logo、不放浮水印 |
| 02 | 三大痛點 | 45 | 三張等高痛點卡，編號放大。**只列問題不列解法**——解法留給講者口頭帶，以及 05–07 的實圖 |
| 03 | NT$999 買到什麼 | 45 | 數字滾動 + 五項 checklist + 資料來源欄位 |
| 04 | ★ 四條路比一比 | 60 | 全場最有說服力的一頁。表格右上蓋「觀測貿易數據／經濟實惠首選」實心章 |
| 05 | 用法 01　市場掃描 | 40 | 配圖 `dashboard-value.jpg` |
| 06 | 用法 02　趨勢監控 | 40 | 右側 inline SVG 折線圖，兩條線從左往右長出來 |
| 07 | 用法 03　價格防禦 | 40 | 配圖 `dashboard-price.jpg` + 談判對白 |
| 08 | 申請流程與資格 | 30 | 六步驟橫排一列 + 七項資格 chips 一列 |
| 09 | 立即報名 | 25 | 年度限量 300 家 + 大張報名 QR + 諮詢窗口 + 主辦／執行 logo，蓋「額滿即止」章 |

每頁講者備註開頭都標了建議秒數，按 `N` 打開。

## 相對 16 頁版的取捨

| 處理 | 內容 | 理由 |
|---|---|---|
| 刪除 | Part 1 全部（AiEZ TRADE 平台、EZ 助手、加值服務、LINE@，原 01–07 頁） | 本場只講數據看板 |
| 沿用 | 原 09 頁「三大痛點」＝新的第 02 頁 | 曾試過把三個痛點與三種用法做成上下對照卡，但等於先把 05–07 頁的解法劇透完，那三頁就沒力了。改回只列問題，解法由講者口頭帶到 |
| 沿用 | 原 15 頁「申請流程與資格」、原 16 頁「立即報名」＝新的第 08、09 頁 | 曾試過合併成一頁，但六步驟被壓成 3×2、QR 也放不大，最後仍拆回兩頁 |
| 刪除 | 原 08 頁章節轉場 | 獨立成一份簡報後，轉場頁的功能由封面承擔 |
| 改寫 | 「應用場景 01/02/03」→「用法 01/02/03」 | 與第 02 頁的「三種用法對應」用同一個詞，前後扣得起來 |
| 新增 | 螢光筆重點（`.mark`） | 每頁標題只畫一處，5 分鐘的場子需要更強的視覺定錨 |
| 新增 | 裝訂條由上往下長出來 | 翻頁有節奏感，且效果從「報單」這個主題長出來 |
| 新增 | 主辦／執行 logo 列（第 09 頁） | 經濟部國際貿易署＋外貿協會，只放結尾一處 |
| 更換 | 報名 QR 改為高雄辦事處專屬表單 | 原本是通用表單，兩者是不同的 Form |
| 更新 | 眉標一律改為「高雄辦事處」 | — |
| 移除 | 封面的 999 巨大浮水印 | 試過，畫面偏怪；封面回到乾淨的標題＋四格條件 |
| 移除 | 封面的 logo 列 | 封面只留文字署名（主辦／執行），logo 集中放在最後一頁 |

## 檔案

```
index.html                       單檔簡報（HTML/CSS/JS 全內嵌），共 9 頁
assets/dashboard-value.jpg       第 05 頁　出口金額統計
assets/dashboard-price.jpg       第 07 頁　出口單價統計
assets/qr-form-kaohsiung.png     第 09 頁　高雄辦事處報名表單 QR
assets/logo-ita.png              第 09 頁　經濟部國際貿易署
assets/logo-taitra.png           第 09 頁　中華民國對外貿易發展協會
assets/dashboard-overview.png    備用（產業總覽儀表板），目前未使用
assets/_unused/                  已停用素材，勿再引用
```

### 素材處理

- **報名 QR**：原始檔 1890×1890，縮到 900×900 存檔。縮圖後實測仍可正確解碼，
  指向 `https://forms.cloud.microsoft/r/JvDJNXkVc7`（**高雄辦事處專屬表單**）。
  16 頁版用的通用表單 QR（`8ngfPR3vkc`）已移到 `assets/_unused/`，兩者是不同入口，不要混用。
- **兩枚 logo**：原始檔四周都有大量透明留白（TAITRA 原檔 4334×4334，實際內容只占中間一條），
  已依 alpha 通道裁到內容邊界，再統一正規化成 120px 高。CSS 用 `height` 控制、`width:auto`，
  兩枚 logo 的視覺重量才會一致。
- 兩枚 logo 都是**深色去背**檔：現在放在淺色紙面上，直接放即可（`.logobar.plain`）。
  若日後要移到深藍頁，改用不加 `.plain` 的版本墊白底，否則深色 logo 會整個吃進背景裡。

## 技術說明

- 單一 HTML 檔，無外部相依套件，離線可用（字型 fallback 到系統的微軟正黑體／PingFang TC）
- 橫向 scroll-snap 換頁，`prefers-reduced-motion` 下自動關閉所有動態
- 第 06 頁的趨勢折線圖是 inline SVG，不是圖檔
- 圖檔一律用 ASCII 檔名，避免中文路徑的編碼問題
- 已驗證 1024×768、1280×720、1366×768、1600×900、1920×1080 五種投影尺寸下 9 頁均不溢出

### 失效防護（改版時務必保留）

- 動畫一律 `animation-fill-mode: backwards` 而非 `both`。backwards 只在「延遲期間」套用起始狀態，
  結束後回到元素本來可見的樣子；若用 `both`，動畫被瀏覽器節流或中斷時內容會**永遠停在 opacity:0**，整頁空白。
- 螢光筆用 `background-image` 圖層而非 `::before` 疊層，靜止狀態就是「已畫滿」，動畫沒跑也看得到。
- 印章的 `visibility` 由 `.stamped` class 直接控制，不靠動畫；動畫沒跑印章仍然蓋得出來。
- 數字滾動另有兩道保險：分頁不可見時（`document.hidden`）直接顯示正確值不跑動畫；
  切換分頁時把滾動中的數字補成最終值，避免畫面停在滾動途中的假價格。
- `.seal.seal-pin` 用雙 class 提高權重，否則會被後面動畫區塊的 `.seal{position:relative}` 蓋掉（同權重、後者勝）。

### 轉 PDF 的三個地雷（都已修好，改版時別踩回去）

1. **列印時必須關掉所有動畫**（`@media print` 裡的 `animation:none !important`）。
   第 1 頁載入會跑逐項揭示，列印快照剛好抓在 `animation-delay` 期間——`backwards` 的起始狀態是
   `opacity:0`，整頁內容會憑空消失，印出來只剩頁首和空框線。
2. **窄螢幕的 media query 一定要限定 `screen`**（`@media screen and (max-width:900px)`）。
   否則轉 PDF 時會誤中手機版規則，兩欄版面全部塌成單欄、內容擠爆頁面。
3. **`print-color-adjust:exact` 不能少**。瀏覽器列印預設會拿掉背景色，
   而海關藍封面、紅色印章、表格反白格全都靠背景，少了這行印出來是一片白紙。
